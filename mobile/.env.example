# Copy to .env — Expo only reads EXPO_PUBLIC_* at startup.
# Use your PC LAN IP, NOT localhost (phone can't reach localhost).
EXPO_PUBLIC_API_URL=http://192.168.1.5:8000/api
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
